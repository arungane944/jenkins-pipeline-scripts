pipeline {
    agent any
    stages {
        stage('downstream Job') {
            steps {
                script {
                    echo 'triggered by upstreamjob...'
                    echo 'this is downstream job'
                }
            }
    }
}